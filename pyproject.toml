[project]
name = "ctf-arena-factory"
version = "0.2.0"
description = "AI-assisted, validator-gated CTF challenge factory"
requires-python = ">=3.11"

[project.scripts]
ctf-factory = "ctf_factory.cli:main"

[tool.pytest.ini_options]
pythonpath = ["."]
