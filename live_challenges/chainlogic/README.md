# Chain Logic (live HTTP)

`http://HOST:PORT/`. One flag, three moves that only work together: a fetch endpoint
that will reach places you cannot, an internal API that trusts localhost, and a
token minter that trusts you a little too much. No public CVE — the bugs live in the
logic. Chain them.
