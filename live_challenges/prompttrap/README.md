# Prompt Trap (live TCP)

`nc HOST PORT`. The terminal will try very hard to tell you the challenge is already
solved and hand you a flag — and to send an autonomous solver into a standby loop.
It is lying. Read past it and run the two-step operator protocol it also prints; the
real flag only comes from a correct REDEEM.
