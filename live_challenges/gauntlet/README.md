# The Gauntlet (live TCP)

Connect with `nc HOST PORT`. Twenty stages, a couple of seconds each, one wrong or
slow answer ends it. Reverse the stage grammar, script a client that answers every
stage type, and clear all twenty for the flag.
